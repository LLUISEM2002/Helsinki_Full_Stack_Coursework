```mermaid
sequenceDiagram
    participant browser
    participant server

    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa
    activate server
    server->>browser: Code 201 with new note as JSON
    deactivate server

    NOTE right of browser: the currently loaded JS program loads the newly received/updated JSON entry.
```